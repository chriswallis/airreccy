# Airreccy local dev

Run locally with Docker Desktop:

```bash
docker compose up --build
```

Then open:

http://localhost:8080/

This serves the project directly from the working directory, so edits are reflected immediately without rebuilding the container.

To stop it:

```bash
docker compose down
```
